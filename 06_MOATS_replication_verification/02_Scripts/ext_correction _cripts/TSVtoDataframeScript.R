##Hello World

##R script created to record and sampling of the bits of code I think'll need to read the MOATS TSVs and use them to plot activity inside each MOATs









## The data frame I want to get to
#     how to make R read in the TSV this way...
## d <- data.frame(moats = character(), dateTime = character(), aTemperature = numeric(), 
##                 sTemperature = numeric(), pH = numeric(), DO = numeric(), salinity = numeric(),
##                 stringsAsFactors = FALSE)
#d <- NULL